import React from 'react';
import { View } from 'react-native';

import { theme } from '../../global'

export const User = () => {
    return <View style={{ flex: 1, backgroundColor: theme.colors.lightWhite }} />;
}